name: Deploy Backend to Cloud Run

on:
  workflow_run:
    workflows: ["Build and Push Backend Docker Image"]
    types:
      - completed
    branches:
      - main
  workflow_dispatch:

env:
  PROJECT_ID: hosting-vms
  REGION: us-central1
  SERVICE_NAME: campusfix-backend
  REGISTRY_IMAGE: ghcr.io/${{ github.repository }}-backend:latest

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write
      deployments: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Start GitHub Deployment
        uses: chrnorm/deployment-action@v2
        id: deployment
        with:
          token: "${{ github.token }}"
          environment: production
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Lowercase image name
        run: |
          IMAGE_NAME="ghcr.io/${{ github.repository }}-backend"
          echo "IMAGE_NAME=${IMAGE_NAME,,}:latest" >> $GITHUB_ENV

      - name: Deploy to Cloud Run
        id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          region: ${{ env.REGION }}
          image: ${{ env.IMAGE_NAME }}
          flags: --port=8021 --allow-unauthenticated --project=${{ env.PROJECT_ID }}
          env_vars: |
            MONGO_URI=${{ secrets.MONGO_URI }}
            DB_NAME=${{ secrets.DB_NAME }}
            FLASK_DEBUG=${{ secrets.FLASK_DEBUG }}
            OUTLOOK_EMAIL=${{ secrets.OUTLOOK_EMAIL }}
            OUTLOOK_PASSWORD=${{ secrets.OUTLOOK_PASSWORD }}
            OUTLOOK_HOST=${{ secrets.OUTLOOK_HOST }}
            OUTLOOK_PORT=${{ secrets.OUTLOOK_PORT }}
            ADMIN_1_EMAIL=${{ secrets.ADMIN_1_EMAIL }}
            ADMIN_1_PASSWORD=${{ secrets.ADMIN_1_PASSWORD }}
            ADMIN_1_NAME=${{ secrets.ADMIN_1_NAME }}
            MICROSOFT_CLIENT_ID=${{ secrets.MICROSOFT_CLIENT_ID }}

      - name: Update deployment status (success)
        if: success()
        uses: chrnorm/deployment-status@v2
        with:
          token: "${{ github.token }}"
          environment-url: ${{ steps.deploy.outputs.url }}
          state: "success"
          deployment-id: ${{ steps.deployment.outputs.deployment_id }}

      - name: Update deployment status (failure)
        if: failure()
        uses: chrnorm/deployment-status@v2
        with:
          token: "${{ github.token }}"
          state: "failure"
          deployment-id: ${{ steps.deployment.outputs.deployment_id }}

      - name: Show Output
        run: echo ${{ steps.deploy.outputs.url }}
