services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8021:8021"
    env_file:
      - ./backend/.env
    environment:
      - FLASK_ENV=production
    restart: always

networks:
  default:
    name: campusfix-network
